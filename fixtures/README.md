# xgithub S0 synthetic fixtures

This public repository contains synthetic code only. It exists to exercise OAuth, review, pagination, anchor and large-pull-request behavior for xgithub. No production repository, customer data or real credentials belong here.

Branches and pull requests are named by fixture. The fixture manifest is intentionally public and contains only immutable commit and pull-request identifiers after creation.
